#!/usr/bin/expect -f
#
# spacial [] {} etc. can't be put after telnet
#
if { $argc == 0 } {
    set dest /home/test4g/
} else {
    set dest [ lrange $argv 0 0 ]
}

puts "dest: $dest"

spawn /root/bin/.4bkup.sh $dest

expect  "password:"
send    "zj12345\n"

interact { timeout 30 { send "" } }   # no interact

puts "end"
