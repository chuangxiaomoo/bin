# tree

H264.句法元素
组织结构
书本内容结构
进程树
文件目录树
main()调用栈

# 二叉树.visit

算法的`名字`很重要，三个算法的命名，少了主语(参照物)，全称应该为:

* 根结点先序
* 根结点中序
* 根结点后序

核心思想有两点:

* 以根结点为主语(参照)
* 先左后右

先序(前序: pre-order)遍历： 
    1.访问根节点 
    2.先序遍历左子树 
    3.先序遍历右子树 
中序(in-order)遍历： 
    1.中序遍历左子树 
    2.访问根节点 
    3.中序遍历右子树 
后序(post-order)遍历： 
    1.后序遍历左子树 
    2.后序遍历右子树 
    3.访问根节点

