# 系统配置

一、通常，系统配置提高了系统的灵活性。《UNIX编程艺术》


二、常用配置方法

    U-boot bootargs -> /proc/cmdline

    /etc/ini配置文件

    argc argv 命令行参数

    /sys/net proc文件系统实时配置

    jcpcmd 通过网络实时配置

# 数据与UI分离.devveopt

1 当前值
2 默认值
3 范围

这些都在存储在 config 中，以做到维护方便。

比如，每一个`分辨率`都要有对应的 {fps,bps,gop}，在不同的产品上，这些默认值都不同。

