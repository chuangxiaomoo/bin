
# pip 配置文件

%APPDATA%\pip\pip.ini
pip list --format=columns

# 模块安装

**安装模块**
which pip pip3
md5sum pip pip3                       *md5sum完全相同*

**pip list**                          *查看已安装列表*

pip install pymysql

pip install lxml
pip install requests
pip install bs4
pip install Tushare

