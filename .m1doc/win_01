
零、cmd

    # del all Desk_top_.ini in the disc e without interaction;
    del e:\Desk_top_.ini /f/s/q/a

    # copy dir
    xcopy /s /e /i /y svnmodification 123 

    # force the PC shutdown without warnning!
    shutdown -s -f -t [secs]	

    services.msc

    msconfig

    “运行” -> “dxdiag” -> “Directx诊断工具”->
            “显示” -> “当前显示模式” -> 1024 x 768 (32bit)
            “系统” -> CPU 内存等系统信息

一、没有usb弹出图标
    RunDll32.exe shell32.dll,Control_RunDLL hotplug.dll

二、电脑出厂信息
    systeminfo 

三、添加printer
    add printer -> next -> choose local printer connect to this PC ->
    next -> create new port(standar tcp/ip port) -> install the driver
    (maybe you can download it on the server) -> IP -> OK

四、 visio
    0. press middle to move the scheme"
    a. figure > other visio scheme > connecting line
    b. shape > spin or revert >  [ctrl + R]
    c. view > zoom > 
        view > shape window;
        view > toolbar > common
    d. select > right click > combine or uncombine.
    e. click logo draw to free the draw hand.

五、VLC
  1.
    VLC need to connect in an interval of 5 minutes

    tools -> preference -> show setting all -> input / coder -> separator
        -> RTP/RTSP

  2.
    <Ctrl-N> rtsp://192.168.2.44/1/d1
