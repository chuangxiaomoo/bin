# convention 约定 

## 可选参数如 [bnum]

    lots of commands are variable parameter commands, so default come along

    commands [bnum]         
    ... command-list ...
    end


## 配对命令 set show

    set  listsize <count>
    show listsize

